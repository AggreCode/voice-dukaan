# LAN HTTPS for phone testing. Replace the IP with your laptop's LAN IP,
# then install Caddy's root CA on the phone (or use `cloudflared tunnel` instead).
192.168.1.20 {
    tls internal
    handle /api/* {
        reverse_proxy backend:8000
    }
    handle {
        reverse_proxy frontend:5173
    }
}
